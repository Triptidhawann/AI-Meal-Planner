export function OptionGroup({ label, hint, options, value, onChange }) {
  return <fieldset><legend className="text-sm font-bold text-ink">{label}</legend>{hint && <p className="mt-1 text-xs text-muted">{hint}</p>}<div className="mt-3 grid grid-cols-2 gap-2 sm:flex sm:flex-wrap">{options.map((option) => <button key={option} type="button" onClick={() => onChange(option)} className={`rounded-xl border px-3 py-3 text-left text-sm font-semibold transition-all sm:px-4 ${value === option ? 'border-saffron bg-saffron/10 text-ink shadow-sm' : 'border-line bg-white text-muted hover:border-saffron/60 hover:text-ink'}`} aria-pressed={value === option}>{option}</button>)}</div></fieldset>;
}
export function EquipmentGroup({ options, value, onChange }) {
  return <fieldset><legend className="text-sm font-bold text-ink">What equipment do you have?</legend><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-3">{options.map((option) => { const selected = value.includes(option); return <button key={option} type="button" onClick={() => onChange(selected ? value.filter((item) => item !== option) : [...value, option])} className={`rounded-xl border px-3 py-3 text-left text-sm font-semibold transition-all ${selected ? 'border-sage-deep bg-sage text-sage-deep' : 'border-line bg-white text-muted hover:border-sage-deep/50 hover:text-ink'}`} aria-pressed={selected}><span className={`mr-2 inline-block h-2 w-2 rounded-full ${selected ? 'bg-sage-deep' : 'bg-line'}`} />{option}</button>; })}</div></fieldset>;
}
