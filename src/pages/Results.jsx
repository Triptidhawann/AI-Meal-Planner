import { ArrowLeft, RotateCcw, SlidersHorizontal } from 'lucide-react';
import { Link } from 'react-router-dom';
import MealCard from '../components/MealCard';
import { useMeals } from '../context/MealContext';

export default function Results() {
  const { results, preferences } = useMeals();
  return <main className="mx-auto max-w-6xl px-5 py-12 sm:py-16"><Link to="/plan" className="inline-flex items-center gap-2 text-sm font-bold text-muted hover:text-ink"><ArrowLeft size={16} /> Edit preferences</Link><div className="mt-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-saffron">Your recommendations</p><h1 className="mt-3 font-display text-5xl leading-tight text-ink">Meals that fit your kitchen.</h1><p className="mt-3 text-base leading-7 text-muted">Based on your ingredients, preferences, and constraints.</p></div><Link to="/plan" className="inline-flex items-center justify-center gap-2 rounded-full border border-line bg-white px-5 py-3 text-sm font-bold text-ink hover:border-ink"><RotateCcw size={16} /> Start new search</Link></div><div className="mt-9 flex flex-wrap gap-2 rounded-2xl border border-line bg-white p-4 text-xs font-semibold text-muted shadow-card"><span className="inline-flex items-center gap-1.5 rounded-full bg-sage px-3 py-2 text-sage-deep"><SlidersHorizontal size={14} /> Your brief</span><span className="rounded-full bg-cream px-3 py-2">{preferences.ingredients.join(', ')}</span><span className="rounded-full bg-cream px-3 py-2">{preferences.time || 'Any time'}</span><span className="rounded-full bg-cream px-3 py-2">{preferences.budget}</span><span className="rounded-full bg-cream px-3 py-2">{preferences.diet}</span></div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{results.map((meal) => <MealCard key={meal.id} meal={meal} />)}</div></main>;
}
